services:
 - type: web
   name: node-render-api
   runtime: node
   plan: free
   buildCommand: npm install
   startCommand: npm start
   envVars:
    - key: DATABASE_URL
      fromDatabase:
        name: node-db
        property: connectionString

databases:
 - name: node-db
   plan: free
